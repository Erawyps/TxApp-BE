/**
 * Ce fichier est réservé pour l'utilisation côté serveur uniquement
 * Prisma ne peut pas s'exécuter dans l'environnement du navigateur
 *
 * Pour l'authentification côté client, utilisez AuthService qui fait des appels API
 */

// Ce fichier ne doit être importé que côté serveur (Cloudflare Worker, Node.js, etc.)
console.warn('⚠️  prisma.js ne doit être utilisé que côté serveur. Utilisez AuthService pour les appels API côté client.');

export default null;
